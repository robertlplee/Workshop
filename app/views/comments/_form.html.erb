<%= form_for ([@lesson, @comment]) do |f| %>
    <p>
        <%= f.label :commenter %><br>
        <%= f.text_field :commenter %>
    </p>
    
    <p>
        <%= f.label :body %><br>
        <%= f.text_field :body %>
    </p>
    
    <p>
        <a class="btn btn-success"><%= f.submit %></a>
    </p>
<%end%>

<% link_to 'Back', lessons_path %>