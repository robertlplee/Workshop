<h1><%= @lesson.name %></h1>
<h2> Hosted by: <%=@lesson.host.email%></h2>

<p>This workshop will be from <%=@lesson.StartTime%> to <%=@lesson.EndTime%> at <%=@lesson.location%>.</p>
<p>
    Description:</br>
    <%=@lesson.description%>
</p>

<p>Users in this workshop:</br>
	<% @lesson.users.each do |student| %>
	<%= student.email %>
	<% end %>
</p>


<% if current_user == @lesson.host %>
    <%= link_to "Edit workshop details", edit_lesson_path %> | 

    <%= link_to 'Destroy', lesson_path(@lesson),
				method: :delete, 
				data: { confirm: 'Are you sure?' } %> | 
<% else %>
	<%= link_to "Join workshop", add_student_lesson_path(@lesson) %>
<% end %>

	


<%= link_to 'Back to Lessons', lessons_path %>


<%= render '/comments/form' %>
<h2>Comments:</h2>
<%= render @all_comments %>